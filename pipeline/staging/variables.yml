# Variables template for dev environment inferno-ui-avatars-holox-standalone-queue

variables:
  #user-defined variables
  pipelineRef: refs/head/master #pipeline repo git reference if not master

  #pipeline details
  vmImageName: 'ubuntu-latest'
  adoProject: ignitex

  #environment details
  environmentName: staging
  adoEnvironment: ignitex-staging
  azureSubscription: 'azure_staging_subscription'
  clusterName: 'nt-ar-staging-us'
  clusterRG: 'nt-ar-staging-us-rg'
  namespace: core

  # Service Details
  serviceName: inferno-ui-avatars
  repository_name: inferno-ui-avatars

  # Docker details
  dockerFilePath: '**/Dockerfile'
  dockerRegistryServiceConnection: 'infernocoreprd'
  dockerBuildArg: 
